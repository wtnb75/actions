// Package dummy documents the module root so bare `go get -u` (no wildcard)
// has a package to resolve when this fixture is used to exercise composite
// actions that assume a root-level package (e.g. goupdate).
package dummy
